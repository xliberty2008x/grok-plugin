interface:
  display_name: "Grok Review"
  short_description: "Review current Git changes with Grok"
  default_prompt: "Use $grok:review to review my current Git changes."
policy:
  allow_implicit_invocation: true
